name: _auto-label
on:
  workflow_call:
    inputs:
      config_repo: { type: string, default: kleffio/.github }
      config_ref: { type: string, default: main }
      unified_path: { type: string, default: configs/labels.yml }

permissions:
  contents: read
  pull-requests: write

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - uses: kleffio/.github/.github/actions/labels-prepare@main
        id: prep
        with:
          config_repo: ${{ inputs.config_repo }}
          config_ref: ${{ inputs.config_ref }}
          unified_path: ${{ inputs.unified_path }}

      - name: Apply labels
        uses: actions/labeler@v5
        with:
          configuration-path: ${{ steps.prep.outputs.labeler_path }}
          repo-token: ${{ secrets.GITHUB_TOKEN }}
