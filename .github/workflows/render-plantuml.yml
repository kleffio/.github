name: Render PlantUML Diagrams

on:
  push:
    branches: [ "main" ]
    paths:
      - "**/*.puml"
      - "**/*.plantuml"
      - ".github/workflows/render-plantuml.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  render:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Download PlantUML
        run: |
          mkdir -p tools
          curl -L -o tools/plantuml.jar https://github.com/plantuml/plantuml/releases/latest/download/plantuml.jar

      - name: Render diagrams to SVG
        run: |
          mkdir -p docs/diagrams
          # Render every .puml/.plantuml file found in the repo into docs/diagrams
          while IFS= read -r file; do
            echo "Rendering $file"
            java -jar tools/plantuml.jar -tsvg "$file" -o "$(pwd)/docs/diagrams"
          done < <(find . -type f \( -name "*.puml" -o -name "*.plantuml" \))

      - name: Commit rendered diagrams
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/diagrams
          git commit -m "chore(diagrams): render PlantUML" || echo "No changes to commit"
          git push